language: rust

rust:
  - nightly

cache:
  - cargo

install:
  - sudo apt-get install grub-common
  - cargo install xargo

script:
  - make test

addons:
  apt:
    packages:
      - nasm
      - libssh2-1
      - xorriso
